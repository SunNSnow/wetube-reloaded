extends base
include mixins/video

block content
  form(method="GET")
    input(placeholder="Search by Title", name="keyword", type="text")
    button Search Now

  each video in videos 
    +video(video)